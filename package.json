{
  "name": "sk-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "preview": "vite preview",
    "checa": "node scripts/checa-conta.mjs",
    "deploy": "npm run checa && vite build && wrangler deploy",
    "migra": "npm run checa && wrangler d1 migrations apply sk-crm-db --remote"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "wrangler": "^4.113.0"
  }
}
